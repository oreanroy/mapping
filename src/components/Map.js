import React, {Component} from "react";

export default class Map extends Component {
    render() {
        return(
            <div className="map">
                <h1>This area will have map once i add the google maps api</h1>
            </div>
        )
    }
}